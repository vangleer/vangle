export * from './src/<%= fileName %>'
import { withInstall } from '@vangle/utils'
import type { App } from 'vue'
import <%= name %> from './src/<%= fileName %>.vue'

  <%= name %>.install = (app: App) => {
    app.component(<%= name %>.name, <%= name %>)
  }

export const Van<%= name %> = withInstall(<%= name %>)

export default Van<%= name %>